---
--- 
<div class="m-[5px] shadow-xl rounded-xl bg-white">
  <section class="pt-8 pb-16 px-6 text-center md:text-left md:px-20 flex flex-col md:flex-row items-center justify-between">
 <section id="contacto" class="bg-gray-50 py-16 px-6 md:px-20 text-center">
  <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-6">
    ¿Tienes dudas o quieres unirte a <span class="text-sky-500">HealthUnity</span>?
  </h2>
  <p class="text-gray-600 max-w-2xl mx-auto mb-12">
    Ya seas paciente, consultorio o clínica, estamos aquí para ayudarte. 
    Escríbenos y te mostraremos cómo transformar la gestión de tus citas médicas.
  </p>

  <div class="max-w-lg mx-auto bg-white shadow-md rounded-2xl p-6 text-left">
    <form action="#" method="POST" class="space-y-4">
      <div>
        <label for="nombre" class="block text-sm font-semibold text-gray-700 mb-1">Nombre completo</label>
        <input
          type="text"
          id="nombre"
          name="nombre"
          placeholder="Tu nombre"
          class="w-full border border-gray-300 rounded-lg p-3 text-sm focus:ring-2 focus:ring-sky-500 focus:outline-none"
          required
        />
      </div>

      <div>
        <label for="email" class="block text-sm font-semibold text-gray-700 mb-1">Correo electrónico</label>
        <input
          type="email"
          id="email"
          name="email"
          placeholder="tucorreo@ejemplo.com"
          class="w-full border border-gray-300 rounded-lg p-3 text-sm focus:ring-2 focus:ring-sky-500 focus:outline-none"
          required
        />
      </div>

      <div>
        <label for="mensaje" class="block text-sm font-semibold text-gray-700 mb-1">Mensaje</label>
        <textarea
          id="mensaje"
          name="mensaje"
          rows="4"
          placeholder="Cuéntanos cómo podemos ayudarte..."
          class="w-full border border-gray-300 rounded-lg p-3 text-sm focus:ring-2 focus:ring-sky-500 focus:outline-none"
          required
        ></textarea>
      </div>

      <button
        type="submit"
        class="w-full bg-sky-500 text-white font-semibold py-3 px-6 rounded-lg shadow-md hover:bg-sky-600 transition-all"
      >
        Enviar mensaje
      </button>
    </form>
  </div>

  <div class="mt-10 text-gray-600 text-sm">
    <p>📍 Bogotá, Colombia</p>
    <p>📞 +57 300 123 4567</p>
    <p>✉️ contacto@healthunity.com</p>
  </div>
</section>
  </section>
</div>

